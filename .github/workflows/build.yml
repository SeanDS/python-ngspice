name: Build and test python-ngspice

on: [push]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-linux:
    name: Build for Python ${{ matrix.python-version }} on Linux
    runs-on: ubuntu-latest
    container: quay.io/pypa/manylinux_2_24_x86_64
    env:
      # Need to use manylinux image's special Python binaries for each version.
      python: python${{ matrix.python-version }}

    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.8"
          - "3.9"
          - "3.10"

    steps:
    # ngspice is not available as a prebuilt package on Debian 9, which is what manylinux_2_24 uses.
    # This uses the Debian repo for ngspice, which has better tagging than the official one :-/.
    - name: Build and install libngspice development files
      run: |
        apt-get update
        apt-get install -y gcc bison flex libtool autoconf automake git
        cd /tmp
        git clone https://salsa.debian.org/electronics-team/ngspice.git
        cd ngspice
        git checkout debian/bullseye
        chmod +x compile_linux_shared.sh
        ./autogen.sh
        if [ $? -ne 0 ]; then  echo "./autogen.sh failed"; exit 1 ; fi
        ./compile_linux_shared.sh --enable-relpath

    - name: Install Python build dependencies
      run: ${{ env.python }} -m pip install build

    - name: Package list
      run: ${{ env.python }} -m pip list installed

    - name: Get source code
      uses: actions/checkout@v2

    - name: Create wheel
      run: |
        ${{ env.python }} -m build -o dist --wheel

    - name: Delocate shared objects
      run: auditwheel repair -w wheelhouse dist/*.whl

    - name: Archive dist artifacts
      uses: actions/upload-artifact@v2
      with:
        name: wheel-python-${{ matrix.python-version }}
        path: wheelhouse

  test-linux:
    name: Test for Python ${{ matrix.python-version }} on Linux
    runs-on: ubuntu-latest
    needs: build-linux

    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.8"
          - "3.9"
          - "3.10"

    steps:
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Download built ngspice wheel
      uses: actions/download-artifact@v2
      with:
        name: wheel-python-${{ matrix.python-version }}
        path: wheelhouse

    - name: Install wheel
      run: |
        python -m pip install wheelhouse/*.whl
        python -c "import ngspice; print(ngspice.__version__)"

    - name: Install Python testing dependencies
      run: python -m pip install pytest

    - name: Package list
      run: python -m pip list installed

    - name: Get source code
      uses: actions/checkout@v2

    - name: Remove repository package
      run: rm -r ngspice

    - name: Run tests
      run: python -m pytest
